
# A simple project just to test buildpro
# To start build run:
#   ./buildpro.py buildpro_test

compiler: g++

sources:
  - buildpro_test.cc

artifact:
  name: buildpro_test

#command:
#  gcc -c {input} -o {artifact.name}
